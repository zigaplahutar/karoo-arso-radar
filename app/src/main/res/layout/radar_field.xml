<?xml version="1.0" encoding="utf-8"?>
<!-- Ta layout Karoo OS izrise v svojem procesu prek RemoteViews,
     zato sme vsebovati samo osnovne poglede (FrameLayout, ImageView, TextView). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <ImageView
        android:id="@+id/radar_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/datatype_name"
        android:scaleType="fitCenter" />

    <TextView
        android:id="@+id/radar_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="4dp"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/radar_caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:background="#99000000"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:textColor="#FFFFFF"
        android:textSize="10sp"
        android:visibility="gone" />
</FrameLayout>
